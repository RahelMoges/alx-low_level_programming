#include "main.h"
/**

* _isupper - a function that checks for uppercase character
* @ digit 0-9
* @c: input
* Return: if c is uppercase, 0 otherwise
*/

int  _isupper(int c)
{
if ((c >= 'A') && (c <= '2'))
return (1)
else
return(0)
}
